#:schema https://developers.openai.com/codex/config-schema.json

# Repo-specific defaults for the RT1064 embedded workspace:
# keep day-to-day turns strong but not over-heavy,
# and reserve the highest effort for explicit planning work.
model = "gpt-5.4"
model_reasoning_effort = "high"
plan_mode_reasoning_effort = "xhigh"
personality = "pragmatic"
model_verbosity = "medium"
